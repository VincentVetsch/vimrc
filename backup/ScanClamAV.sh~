#!/bin/bash

# Does not work if the file or directory, there are two or more
# standing one by one space, but nested, discharge the same directory
# no name, or one space between words - checked ok

FILE=$1

#add a backslash before the space
VAR=$(echo $FILE | sed -e's:\( \):\\\ :g')
clamscan -r $VAR -i --bell --remove
# If you remove the -i will show you
# + all the files for scanning
# --bell after $ VAR sound when a virus
# add  --removeafter $ VAR remove viruses
exit 0


