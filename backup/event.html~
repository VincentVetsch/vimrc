<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
        <title>How to use Events that work with IE</title>
    </head>
    <body>
        <a href="#" id="anchor">Click This Link</a>
        <div id="box" style="width:200px; height:50px; background:blue">
        </div>
        <script type="text/javascript">
            var anchor = document.getElementById("anchor");
            var box = document.getElementById("box");

            // Callback function that sends an alert
            function sendAlert(e){
                alert("Clicked");
            }
            /* Utility funnction that will add an event for good browsers 
            and bad(IE)*/
            function addEvent(theObject, theEvent, theFunction, capture){
                if (theObject.attachEvent){
                    //This is the event structure for IE
                    theObject.attachEvent("on" + theEvent, theFunction);
                } else {
                    //If the capture variable is not included set it to false
                    if (!capture) capture = false;
                    //This is the event structure for firefox, chrome, safari
                    theObject.addEventListener(theEvent, theFunction, capture);
                }
            }
            addEvent(anchor, 'click', sendAlert);
            addEvent(box, 'click', sendAlert);
        </script>
    </body>
</html>
